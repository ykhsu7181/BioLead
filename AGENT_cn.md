# AGENT_cn.md

本文档是 ScholarLead Agent 项目的中文开发规范，供 Codex 和其他 AI 编程助手参考。

英文版规范文件为 `AGENTS.md`。如果中英文表述不一致，应优先以当前用户指令、`AGENTS.md`、`README.md` 和 `docs/` 下的需求文档为准。

## 项目使命

ScholarLead Agent 是一个海外科研客户发现系统。系统目标不是单纯搜索论文，也不是简单生成邮件，而是帮助用户从关键词或自然语言需求出发，形成有证据支撑的科研客户线索、评分、分析、导出结果，并最终支持经过人工审核的学术邮件触达。

长期业务流程为：

```text
用户输入
→ 检索任务
→ 公开科研数据采集
→ 原始数据保存
→ 数据清洗与标准化
→ 科研人员 / PI 识别
→ 实体去重与归并
→ 论文 / 基金 / 机构 / 邮箱补全
→ 客户评分
→ 客户优先级划分
→ 客户分析
→ 个性化学术邮件草稿
→ 人工审核
→ 确认发送
→ 跟进 / 报告 / 导出
```

开发时不要只优化某一个孤立模块，而忽略完整业务闭环。

## 当前开发策略

先建设确定性工具链，再接入 Agent 编排。

推荐顺序：

```text
官方数据 API
→ 原始响应保存
→ 标准化记录
→ 来源证据
→ 测试
→ 业务规则
→ 工作流集成
→ 可选 LLM / Agent 层
```

当前第一个真实模块是 OpenAlex 论文采集。建议下一个主链路是 PubMed。

除非用户明确要求进入对应阶段，否则不要引入：

- LLM
- Streamlit
- 数据库
- Crossref
- 邮件发送

## 需求来源

项目决策应参考：

- `README.md`
- `README_cn.md`
- `docs/` 下的规划文档
- 海外 Agent 需求与验收标准
- 当前对话中的用户明确指令

如果需求之间出现冲突：

1. 不要擅自修改需求。
2. 记录冲突点。
3. 优先做成可配置实现。
4. 当业务规则不明确时，先向用户确认。

## Python 开发规则

- 支持 Python 3.11 及以上版本。
- 当前本地虚拟环境名称为 `literature_env`。
- 使用现有 `src` 项目结构。
- 主包名为 `scholarlead_agent`。
- 代码应清晰、易读、适合初学者理解。
- 公共函数应添加类型注解。
- 函数保持小而可测试。
- 只有在逻辑不明显时才添加注释。
- 避免无关重构。

## 安全规则

- 不得在代码中写入密码、API Key、SMTP 凭据、OAuth Token 或数据库密码。
- 密钥从环境变量或本地 `.env` 读取。
- 不提交 `.env`。
- `.env.example` 只保留占位示例。
- 日志中不得暴露密钥。

## 数据规则

- 优先使用官方 API，不优先做网页抓取。
- 原始 API 响应必须先保存，再清洗。
- 尽量保留数据来源证据。
- 记录信息来源。
- 不猜测缺失作者邮箱。
- 不编造 ORCID、机构、基金、基金金额、作者角色或研究方向。
- 不把推断信息当成已确认事实。
- 不确定的数据应标记为需要人工审核。

推荐来源字段：

```text
source_name
source_type
source_id
source_url
retrieved_at
```

## 数据源架构规则

外部科研数据源应通过独立 adapter 或清晰分离的 client 模块接入。

推荐接口概念：

```text
search()
fetch_detail()
normalize()
health_check()
```

业务逻辑不应直接依赖某一个第三方 API 的 JSON 结构。

每个外部 API 集成都应处理：

- 超时
- 重试
- 速率限制
- 分页
- 空响应
- HTTP 错误
- 异常响应
- 可处理范围内的 API 字段变化

错误必须可观察、可追踪。后续 API 失败不能导致已经保存的数据丢失。

## 当前 OpenAlex 规则

OpenAlex 采集必须满足：

- 设置清晰的 User-Agent。
- 请求超时时间为 30 秒。
- 对 429 和 5xx 响应最多重试 3 次。
- 第一版 `max_results` 最大限制为 20。
- 正确还原 `abstract_inverted_index`。
- DOI 标准化：去除首尾空格、去除 `https://doi.org/` 前缀、转换为小写。
- 优先按 DOI 去重。
- 没有 DOI 时按 OpenAlex ID 去重。
- 原始响应保存到 `data/raw`。
- 清洗后的 JSON 和 CSV 保存到 `data/processed`。
- 测试中不得访问真实网络。

## 科研人员与客户规则

不要仅凭姓名合并科研人员。

实体匹配应综合考虑：

- 标准化姓名
- ORCID
- 邮箱
- 机构
- 论文重叠
- 基金记录
- 公开主页

去重状态建议支持：

```text
merged
probable_match
manual_review_required
distinct
```

置信度不足时，应交给人工确认。

## 邮箱规则

找到邮箱和证明邮箱属于谁，是两个不同任务。

邮箱与人员匹配必须保留证据链：

```text
email
→ researcher
→ evidence
→ source
```

不得仅凭 HTML、PDF 或纯文本中的位置接近关系，就把邮箱分配给某个客户。

除非用户明确要求进入邮件发送阶段，否则不要实现自动邮件发送。

未来邮件流程必须是：

```text
生成草稿
→ 人工审核
→ 可选修改
→ 明确确认
→ 发送
→ 记录状态
```

## 客户评分规则

默认评分维度：

- 资金活跃度：40%
- 研究方向匹配度：30%
- 发表时效性：20%
- 外包倾向：10%

默认优先级：

- 高优先级：score >= 80
- 中优先级：50 <= score <= 79
- 低优先级：score < 50

权重和阈值必须集中配置，不要散落硬编码在多个代码文件中。

核心数值评分应基于确定性规则，可复现。LLM 可以用于总结证据或解释评分，但不应作为最终数字评分的唯一计算器。

## LLM 与 Agent 规则

只有在确定性数据管道稳定后，再加入 LLM 能力。

加入 LLM 后，相关代码应与以下模块分离：

- 数据源 client
- 数据标准化
- 存储
- 确定性评分

应使用模型供应商抽象层，不要和某一个模型厂商深度绑定。

概念接口：

```text
generate()
model_name
usage
estimated_cost
```

引入 LLM 后，应记录每次调用的 token 用量和估算成本。

## 测试规则

有实际功能变化时，必须新增或更新测试。

数据源模块测试应覆盖：

- 成功响应
- 空响应
- API 错误
- 超时
- 重试行为
- 分页
- 标准化
- 来源字段

除非用户明确要求真实集成测试，否则测试中不得访问真实外部 API。

完成任务前应：

- 运行相关测试。
- 报告修改文件。
- 报告测试结果。
- 报告已知限制。
- 说明是否进行了真实 API 测试。

## 文档规则

用户可见行为变化时，应更新 README。

详细规划材料放在 `docs/`，不要都堆在 README 中。

README 应保持聚焦：

- 项目是什么
- 当前实现了什么
- 当前未实现什么
- 如何安装
- 如何运行
- 如何测试
- 重要安全与数据规则

Markdown 文件使用 UTF-8 编码。避免使用会破坏中文编码的方式重写 Markdown。

## 完成定义

模块不能因为“代码能跑”就算完成。

一个功能真正完成，至少应包含：

```text
代码
+ 测试
+ 异常处理
+ 必要的数据来源证据
+ 必要的配置
+ 文档
+ 接近真实场景的验证
```

如果只运行了模拟测试，必须明确说明。
